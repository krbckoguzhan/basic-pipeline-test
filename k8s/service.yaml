apiVersion: v1
kind: Service
metadata:
  name: basic-pipeline-test-service
spec:
  type: NodePort
  selector:
    app: basic-pipeline-test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30000
